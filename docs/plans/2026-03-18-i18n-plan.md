# Verdaxis i18n Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add full English + Simplified Chinese internationalization to the Verdaxis React app (public website + authenticated platform).

**Architecture:** react-i18next with namespace-based JSON translation files. Public pages use `/:lang/` URL prefixes for SEO; authenticated app uses in-app language toggle persisted to localStorage. Browser language detection as first-pass default.

**Tech Stack:** i18next, react-i18next, i18next-browser-languagedetector

**Codebase:** `/home/verdaxis-prod/verdaxis-frontend`

---

## Task 1: Install dependencies and configure i18next

**Files:**
- Modify: `package.json`
- Create: `src/i18n.ts`
- Modify: `src/index.tsx`
- Modify: `index.html`

**Step 1: Install i18n packages**

Run:
```bash
cd /home/verdaxis-prod/verdaxis-frontend
npm install i18next react-i18next i18next-browser-languagedetector
```

**Step 2: Create i18n configuration**

Create `src/i18n.ts`:

```typescript
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';

// Eager-loaded namespaces
import commonEn from './locales/en/common.json';
import commonZh from './locales/zh/common.json';

export const SUPPORTED_LANGS = ['en', 'zh'] as const;
export type SupportedLang = (typeof SUPPORTED_LANGS)[number];

export function isSupportedLang(lang: string): lang is SupportedLang {
  return (SUPPORTED_LANGS as readonly string[]).includes(lang);
}

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      en: { common: commonEn },
      zh: { common: commonZh },
    },
    fallbackLng: 'en',
    defaultNS: 'common',
    ns: ['common'],
    interpolation: { escapeValue: false },
    detection: {
      order: ['localStorage', 'navigator'],
      lookupLocalStorage: 'verdaxis-lang',
      caches: ['localStorage'],
    },
  });

// Keep <html lang> in sync
i18n.on('languageChanged', (lng) => {
  document.documentElement.lang = lng;
  localStorage.setItem('verdaxis-lang', lng);
});

export default i18n;
```

**Step 3: Create initial common namespace files**

Create `src/locales/en/common.json`:
```json
{
  "lang.en": "English",
  "lang.zh": "中文",
  "btn.save": "Save",
  "btn.cancel": "Cancel",
  "btn.confirm": "Confirm",
  "btn.delete": "Delete",
  "btn.edit": "Edit",
  "btn.close": "Close",
  "btn.submit": "Submit",
  "btn.back": "Back",
  "btn.next": "Next",
  "btn.signIn": "Sign In",
  "btn.signOut": "Sign Out",
  "btn.signUp": "Create Account",
  "btn.applyPilot": "Apply for Pilot",
  "btn.learnMore": "Learn More",
  "btn.viewAll": "View All",
  "loading": "Loading...",
  "error.generic": "Something went wrong. Please try again.",
  "error.network": "Unable to connect to server. Please check your connection.",
  "error.notFound": "Page not found",
  "nav.home": "Home",
  "nav.howItWorks": "How It Works",
  "nav.solutions": "Solutions",
  "nav.fuels": "Fuels",
  "nav.compliance": "Compliance",
  "nav.partners": "Partners",
  "nav.tools": "Tools",
  "nav.education": "Education",
  "nav.roadmap": "Roadmap",
  "nav.forProducers": "For Producers",
  "nav.forBuyers": "For Buyers",
  "nav.forTraders": "For Traders",
  "nav.forFinanciers": "For Financiers",
  "nav.maritime": "Maritime",
  "nav.aviation": "Aviation",
  "nav.landPower": "Land & Power",
  "nav.energyCalculator": "Energy Calculator",
  "nav.producerMap": "Producer Map",
  "nav.privacy": "Privacy Policy",
  "nav.terms": "Terms of Service",
  "nav.governance": "Governance",
  "sidebar.intelligenceMap": "Intelligence Map",
  "sidebar.marketplace": "Marketplace",
  "sidebar.marketTerminal": "Market Terminal",
  "sidebar.myFleet": "My Fleet",
  "sidebar.myTrades": "My Trades",
  "sidebar.watchlists": "Watchlists",
  "sidebar.compliance": "Compliance",
  "sidebar.training": "Training",
  "sidebar.settings": "Settings",
  "sidebar.stats": "Statistics",
  "sidebar.commandCenter": "Command Center",
  "sidebar.quotes": "Quotes",
  "sidebar.analytics": "Analytics",
  "sidebar.admin": "Admin Dashboard",
  "header.search": "Search...",
  "header.searchRequests": "Search requests...",
  "header.platformTour": "Platform Tour",
  "header.notifications": "Notifications",
  "header.profile": "Profile",
  "header.roleBuyer": "Buyer",
  "header.roleSupplier": "Supplier",
  "header.roleAdmin": "Admin",
  "footer.platform": "Platform",
  "footer.solutions": "Solutions",
  "footer.tools": "Tools",
  "footer.tagline": "The compliance-first marketplace for verified sustainable marine fuels.",
  "footer.copyright": "© {{year}} Verdaxis. All rights reserved."
}
```

Create `src/locales/zh/common.json`:
```json
{
  "lang.en": "English",
  "lang.zh": "中文",
  "btn.save": "保存",
  "btn.cancel": "取消",
  "btn.confirm": "确认",
  "btn.delete": "删除",
  "btn.edit": "编辑",
  "btn.close": "关闭",
  "btn.submit": "提交",
  "btn.back": "返回",
  "btn.next": "下一步",
  "btn.signIn": "登录",
  "btn.signOut": "退出",
  "btn.signUp": "注册账户",
  "btn.applyPilot": "申请试用",
  "btn.learnMore": "了解更多",
  "btn.viewAll": "查看全部",
  "loading": "加载中...",
  "error.generic": "出了点问题，请重试。",
  "error.network": "无法连接到服务器，请检查您的网络连接。",
  "error.notFound": "页面未找到",
  "nav.home": "首页",
  "nav.howItWorks": "运作方式",
  "nav.solutions": "解决方案",
  "nav.fuels": "燃料",
  "nav.compliance": "合规",
  "nav.partners": "合作伙伴",
  "nav.tools": "工具",
  "nav.education": "知识中心",
  "nav.roadmap": "路线图",
  "nav.forProducers": "生产商",
  "nav.forBuyers": "采购方",
  "nav.forTraders": "贸易商",
  "nav.forFinanciers": "融资方",
  "nav.maritime": "航运",
  "nav.aviation": "航空",
  "nav.landPower": "陆上电力",
  "nav.energyCalculator": "能源计算器",
  "nav.producerMap": "生产商地图",
  "nav.privacy": "隐私政策",
  "nav.terms": "服务条款",
  "nav.governance": "治理",
  "sidebar.intelligenceMap": "情报地图",
  "sidebar.marketplace": "市场",
  "sidebar.marketTerminal": "交易终端",
  "sidebar.myFleet": "我的船队",
  "sidebar.myTrades": "我的交易",
  "sidebar.watchlists": "关注列表",
  "sidebar.compliance": "合规管理",
  "sidebar.training": "培训",
  "sidebar.settings": "设置",
  "sidebar.stats": "统计",
  "sidebar.commandCenter": "指挥中心",
  "sidebar.quotes": "报价",
  "sidebar.analytics": "数据分析",
  "sidebar.admin": "管理面板",
  "header.search": "搜索...",
  "header.searchRequests": "搜索请求...",
  "header.platformTour": "平台导览",
  "header.notifications": "通知",
  "header.profile": "个人资料",
  "header.roleBuyer": "采购方",
  "header.roleSupplier": "供应商",
  "header.roleAdmin": "管理员",
  "footer.platform": "平台",
  "footer.solutions": "解决方案",
  "footer.tools": "工具",
  "footer.tagline": "合规优先的可持续航运燃料认证交易平台。",
  "footer.copyright": "© {{year}} Verdaxis. 保留所有权利。"
}
```

**Step 4: Initialize i18n in app entry**

Modify `src/index.tsx` — add one import at the top (before App):
```typescript
import './i18n';
```

**Step 5: Verify build**

Run:
```bash
cd /home/verdaxis-prod/verdaxis-frontend && npm run build
```
Expected: Build succeeds, no errors.

**Step 6: Run existing tests**

Run:
```bash
cd /home/verdaxis-prod/verdaxis-frontend && npm run test
```
Expected: All 209 tests pass (no behavioral changes yet).

**Step 7: Commit**

```bash
git add src/i18n.ts src/locales/ src/index.tsx package.json package-lock.json
git commit -m "feat(i18n): add react-i18next with common namespace (en + zh)"
```

---

## Task 2: Add LanguageSelector component

**Files:**
- Create: `src/components/LanguageSelector.tsx`

**Step 1: Create LanguageSelector component**

Create `src/components/LanguageSelector.tsx`:

```tsx
import { useTranslation } from 'react-i18next';
import { Globe } from 'lucide-react';
import { SUPPORTED_LANGS, type SupportedLang } from '../i18n';

interface LanguageSelectorProps {
  /** If provided, called instead of i18n.changeLanguage (used by public pages to navigate). */
  onLanguageChange?: (lang: SupportedLang) => void;
  variant?: 'light' | 'dark';
}

export default function LanguageSelector({ onLanguageChange, variant = 'dark' }: LanguageSelectorProps) {
  const { i18n, t } = useTranslation();

  const handleChange = (lang: SupportedLang) => {
    if (onLanguageChange) {
      onLanguageChange(lang);
    } else {
      i18n.changeLanguage(lang);
    }
  };

  const textColor = variant === 'light' ? 'text-white' : 'text-slate-700 dark:text-slate-200';

  return (
    <div className="relative group">
      <button
        className={`flex items-center gap-1.5 px-2 py-1 rounded-md hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors ${textColor}`}
        aria-label="Change language"
      >
        <Globe size={16} />
        <span className="text-sm font-medium">
          {t(`lang.${i18n.language}`)}
        </span>
      </button>
      <div className="absolute right-0 top-full mt-1 bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-lg shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all z-50 min-w-[120px]">
        {SUPPORTED_LANGS.map((lang) => (
          <button
            key={lang}
            onClick={() => handleChange(lang)}
            className={`w-full text-left px-3 py-2 text-sm hover:bg-slate-50 dark:hover:bg-slate-700 transition-colors first:rounded-t-lg last:rounded-b-lg ${
              i18n.language === lang ? 'font-semibold text-blue-600 dark:text-blue-400' : 'text-slate-700 dark:text-slate-200'
            }`}
          >
            {t(`lang.${lang}`)}
          </button>
        ))}
      </div>
    </div>
  );
}
```

**Step 2: Verify build**

Run: `cd /home/verdaxis-prod/verdaxis-frontend && npm run build`
Expected: Build succeeds.

**Step 3: Commit**

```bash
git add src/components/LanguageSelector.tsx
git commit -m "feat(i18n): add LanguageSelector dropdown component"
```

---

## Task 3: Add `/:lang` routing for public pages

**Files:**
- Create: `src/components/public/LanguageRedirect.tsx`
- Create: `src/components/public/PublicLanguageWrapper.tsx`
- Create: `src/components/public/LegacyRedirect.tsx`
- Modify: `src/App.tsx`

**Step 1: Create LanguageRedirect component**

Create `src/components/public/LanguageRedirect.tsx`:

```tsx
import { Navigate } from 'react-router-dom';
import { useTranslation } from 'react-i18next';
import { isSupportedLang } from '../../i18n';

/**
 * Root "/" — detects language and redirects to /{lang}/
 */
export default function LanguageRedirect() {
  const { i18n } = useTranslation();
  const detected = i18n.language?.split('-')[0] || 'en';
  const lang = isSupportedLang(detected) ? detected : 'en';
  return <Navigate to={`/${lang}/`} replace />;
}
```

**Step 2: Create PublicLanguageWrapper component**

Create `src/components/public/PublicLanguageWrapper.tsx`:

```tsx
import { useEffect } from 'react';
import { Outlet, useParams, Navigate } from 'react-router-dom';
import { useTranslation } from 'react-i18next';
import { isSupportedLang } from '../../i18n';

/**
 * Reads /:lang param, syncs i18n, renders child routes.
 * Invalid lang → redirects to /en/{rest}.
 */
export default function PublicLanguageWrapper() {
  const { lang } = useParams<{ lang: string }>();
  const { i18n } = useTranslation();

  useEffect(() => {
    if (lang && isSupportedLang(lang) && i18n.language !== lang) {
      i18n.changeLanguage(lang);
    }
  }, [lang, i18n]);

  if (!lang || !isSupportedLang(lang)) {
    return <Navigate to={`/en/`} replace />;
  }

  return <Outlet />;
}
```

**Step 3: Create LegacyRedirect component**

Create `src/components/public/LegacyRedirect.tsx`:

```tsx
import { Navigate, useLocation } from 'react-router-dom';
import { useTranslation } from 'react-i18next';
import { isSupportedLang } from '../../i18n';

/**
 * Redirects old un-prefixed public URLs to /{lang}/path.
 * e.g. /how-it-works → /en/how-it-works
 */
export default function LegacyRedirect() {
  const { i18n } = useTranslation();
  const { pathname, search, hash } = useLocation();
  const detected = i18n.language?.split('-')[0] || 'en';
  const lang = isSupportedLang(detected) ? detected : 'en';
  return <Navigate to={`/${lang}${pathname}${search}${hash}`} replace />;
}
```

**Step 4: Restructure routes in App.tsx**

Modify `src/App.tsx`. The key changes:

1. Add imports at top:
```typescript
import LanguageRedirect from './components/public/LanguageRedirect';
import PublicLanguageWrapper from './components/public/PublicLanguageWrapper';
import LegacyRedirect from './components/public/LegacyRedirect';
```

2. Replace the current public route block. The current structure is flat routes like:
```tsx
<Route path="/" element={<PublicLayout><LandingPage /></PublicLayout>} />
<Route path="/how-it-works" element={<PublicLayout><HowItWorksPage /></PublicLayout>} />
```

Replace with:
```tsx
{/* Root → detect language → redirect */}
<Route path="/" element={<LanguageRedirect />} />

{/* Public pages under /:lang */}
<Route path="/:lang" element={<PublicLanguageWrapper />}>
  <Route element={<PublicLayout />}>
    <Route index element={<LandingPage />} />
    <Route path="how-it-works" element={<HowItWorksPage />} />
    <Route path="fuels" element={<FuelCoveragePage />} />
    <Route path="fuels/:sector" element={<FuelCoveragePage />} />
    <Route path="compliance" element={<ComplianceInfoPage />} />
    <Route path="for-producers" element={<ProducerUseCasePage />} />
    <Route path="for-buyers" element={<BuyerUseCasePage />} />
    <Route path="for-traders" element={<TraderUseCasePage />} />
    <Route path="for-financiers" element={<FinancierUseCasePage />} />
    <Route path="governance" element={<GovernancePage />} />
    <Route path="pilot" element={<PilotPage />} />
    <Route path="partners" element={<PartnersPage />} />
    <Route path="partners/:slug" element={<PartnerLandingPage />} />
    <Route path="education" element={<EducationPage />} />
    <Route path="education/:slug" element={<EducationArticlePage />} />
    <Route path="roadmap" element={<RoadmapPage />} />
    <Route path="tools/energy-calculator" element={<EnergyCalculatorPage />} />
    <Route path="map/producers" element={<ProducerMapPage />} />
    <Route path="privacy" element={<PrivacyPage />} />
    <Route path="terms" element={<TermsPage />} />
  </Route>
</Route>

{/* Legacy redirects for old un-prefixed URLs */}
<Route path="/how-it-works" element={<LegacyRedirect />} />
<Route path="/fuels/*" element={<LegacyRedirect />} />
<Route path="/compliance" element={<LegacyRedirect />} />
<Route path="/for-producers" element={<LegacyRedirect />} />
<Route path="/for-buyers" element={<LegacyRedirect />} />
<Route path="/for-traders" element={<LegacyRedirect />} />
<Route path="/for-financiers" element={<LegacyRedirect />} />
<Route path="/governance" element={<LegacyRedirect />} />
<Route path="/pilot" element={<LegacyRedirect />} />
<Route path="/partners" element={<LegacyRedirect />} />
<Route path="/education/*" element={<LegacyRedirect />} />
<Route path="/roadmap" element={<LegacyRedirect />} />
<Route path="/tools/*" element={<LegacyRedirect />} />
<Route path="/map/*" element={<LegacyRedirect />} />
<Route path="/privacy" element={<LegacyRedirect />} />
<Route path="/terms" element={<LegacyRedirect />} />

{/* Auth + app routes — no lang prefix (unchanged) */}
```

**Important:** `PublicLayout` currently wraps each route's children directly. It needs to become an `<Outlet />`-based layout route. Modify `src/components/public/PublicLayout.tsx`:

Change from rendering `{children}` to rendering `<Outlet />`:
```tsx
import { Outlet } from 'react-router-dom';
// ... existing imports ...

export default function PublicLayout() {
  // ... existing Lenis setup ...
  return (
    <>
      <PublicNav />
      <Outlet />
      <PublicFooter />
    </>
  );
}
```

Remove the `children` prop/type if it exists.

**Step 5: Update internal links in PublicNav and PublicFooter**

All `<Link to="/how-it-works">` etc. in PublicNav.tsx and PublicFooter.tsx must become language-aware. Create a helper hook:

Create `src/hooks/useLocalePath.ts`:
```typescript
import { useParams } from 'react-router-dom';

/**
 * Returns a function that prefixes a path with the current /:lang.
 * For use in public page components only.
 */
export function useLocalePath() {
  const { lang } = useParams<{ lang: string }>();
  return (path: string) => `/${lang || 'en'}${path.startsWith('/') ? path : `/${path}`}`;
}
```

Then in PublicNav.tsx and PublicFooter.tsx, replace hardcoded paths:
```tsx
// Before:
<Link to="/how-it-works">
// After:
const lp = useLocalePath();
<Link to={lp('/how-it-works')}>
```

**Step 6: Verify build and manual test**

Run: `cd /home/verdaxis-prod/verdaxis-frontend && npm run build`
Expected: Build succeeds.

Run: `cd /home/verdaxis-prod/verdaxis-frontend && npm run test`
Expected: Tests pass (some public page tests may need route wrapper updates — fix any that break).

**Step 7: Commit**

```bash
git add src/components/public/LanguageRedirect.tsx src/components/public/PublicLanguageWrapper.tsx src/components/public/LegacyRedirect.tsx src/hooks/useLocalePath.ts src/App.tsx src/components/public/PublicLayout.tsx src/components/public/PublicNav.tsx src/components/public/PublicFooter.tsx
git commit -m "feat(i18n): add /:lang URL prefix routing for public pages with legacy redirects"
```

---

## Task 4: Wire LanguageSelector into Header and PublicNav

**Files:**
- Modify: `src/components/layout/Header.tsx`
- Modify: `src/components/public/PublicNav.tsx`

**Step 1: Add LanguageSelector to Header (authenticated app)**

Modify `src/components/layout/Header.tsx`:

Add import:
```typescript
import LanguageSelector from '../LanguageSelector';
```

Place `<LanguageSelector />` in the header bar, before the notification bell or profile dropdown. It should sit in the right-side controls area.

**Step 2: Add LanguageSelector to PublicNav (public pages)**

Modify `src/components/public/PublicNav.tsx`:

Add imports:
```typescript
import { useNavigate, useParams, useLocation } from 'react-router-dom';
import LanguageSelector from '../LanguageSelector';
import { isSupportedLang, type SupportedLang } from '../../i18n';
```

Add a handler that navigates to the same page in the new language:
```typescript
const navigate = useNavigate();
const { lang } = useParams<{ lang: string }>();
const { pathname } = useLocation();

const handleLanguageChange = (newLang: SupportedLang) => {
  // Replace /:lang prefix in current path
  const rest = pathname.replace(/^\/[a-z]{2}/, '');
  navigate(`/${newLang}${rest || '/'}`);
};
```

Place in the nav bar:
```tsx
<LanguageSelector onLanguageChange={handleLanguageChange} variant="light" />
```

**Step 3: Verify build**

Run: `cd /home/verdaxis-prod/verdaxis-frontend && npm run build`

**Step 4: Commit**

```bash
git add src/components/layout/Header.tsx src/components/public/PublicNav.tsx
git commit -m "feat(i18n): wire LanguageSelector into Header and PublicNav"
```

---

## Task 5: Integrate i18n into Sidebar and Header text

**Files:**
- Modify: `src/components/layout/Sidebar.tsx`
- Modify: `src/components/layout/Header.tsx`

**Step 1: Replace hardcoded sidebar labels**

In `Sidebar.tsx`, add:
```typescript
import { useTranslation } from 'react-i18next';
```

In the component body:
```typescript
const { t } = useTranslation();
```

Replace the `sidebarItems` arrays. Change from:
```typescript
{ id: 'MAP', label: 'Intelligence Map', icon: MapIcon }
```
To:
```typescript
{ id: 'MAP', label: t('sidebar.intelligenceMap'), icon: MapIcon }
```

Do this for all sidebar items in both BUYER and SUPPLIER arrays.

**Step 2: Replace hardcoded Header text**

In `Header.tsx`, add `useTranslation` and replace:
- Search placeholder: `t('header.search')` / `t('header.searchRequests')`
- "Platform Tour" button text: `t('header.platformTour')`
- "Sign Out" text: `t('btn.signOut')`
- Role labels: `t('header.roleBuyer')`, `t('header.roleSupplier')`, `t('header.roleAdmin')`

**Step 3: Verify build and test**

Run: `cd /home/verdaxis-prod/verdaxis-frontend && npm run build && npm run test`

**Step 4: Commit**

```bash
git add src/components/layout/Sidebar.tsx src/components/layout/Header.tsx
git commit -m "feat(i18n): translate Sidebar nav labels and Header text"
```

---

## Task 6: Translate PublicNav and PublicFooter

**Files:**
- Modify: `src/components/public/PublicNav.tsx`
- Modify: `src/components/public/PublicFooter.tsx`

**Step 1: Replace PublicNav labels**

Add `useTranslation` and replace all navigation labels:
- "How It Works" → `t('nav.howItWorks')`
- "Solutions" → `t('nav.solutions')`
- "For Producers" → `t('nav.forProducers')`
- etc. for all nav items
- "Apply for Pilot" → `t('btn.applyPilot')`
- "Sign In" → `t('btn.signIn')`

**Step 2: Replace PublicFooter labels**

Add `useTranslation` and replace:
- Column titles: `t('footer.platform')`, `t('footer.solutions')`, `t('footer.tools')`
- All link labels using the same `nav.*` keys
- Tagline: `t('footer.tagline')`
- Copyright: `t('footer.copyright', { year: new Date().getFullYear() })`

Also update footer `<Link to=...>` paths using `useLocalePath()`.

**Step 3: Verify build**

Run: `cd /home/verdaxis-prod/verdaxis-frontend && npm run build`

**Step 4: Commit**

```bash
git add src/components/public/PublicNav.tsx src/components/public/PublicFooter.tsx
git commit -m "feat(i18n): translate PublicNav and PublicFooter"
```

---

## Task 7: Create namespace loader and translate public pages

**Files:**
- Create: `src/locales/en/public.json`
- Create: `src/locales/zh/public.json`
- Modify: `src/i18n.ts` (add lazy namespace loading)
- Modify: `src/pages/public/LandingPage.tsx`
- Modify: all other public page files (21 total)

This is the largest task. It covers all 21 public pages in `src/pages/public/`.

**Step 1: Add namespace lazy-loading to i18n config**

Modify `src/i18n.ts` — add a helper to load namespaces on demand:

```typescript
// Lazy namespace registry — maps ns name to dynamic import
const lazyNamespaces: Record<string, Record<string, () => Promise<{ default: Record<string, string> }>>> = {
  en: {
    public: () => import('./locales/en/public.json'),
    auth: () => import('./locales/en/auth.json'),
    trading: () => import('./locales/en/trading.json'),
    compliance: () => import('./locales/en/compliance.json'),
    dashboard: () => import('./locales/en/dashboard.json'),
    fleet: () => import('./locales/en/fleet.json'),
    ai: () => import('./locales/en/ai.json'),
    education: () => import('./locales/en/education.json'),
    admin: () => import('./locales/en/admin.json'),
    settings: () => import('./locales/en/settings.json'),
    tutorial: () => import('./locales/en/tutorial.json'),
  },
  zh: {
    public: () => import('./locales/zh/public.json'),
    auth: () => import('./locales/zh/auth.json'),
    trading: () => import('./locales/zh/trading.json'),
    compliance: () => import('./locales/zh/compliance.json'),
    dashboard: () => import('./locales/zh/dashboard.json'),
    fleet: () => import('./locales/zh/fleet.json'),
    ai: () => import('./locales/zh/ai.json'),
    education: () => import('./locales/zh/education.json'),
    admin: () => import('./locales/zh/admin.json'),
    settings: () => import('./locales/zh/settings.json'),
    tutorial: () => import('./locales/zh/tutorial.json'),
  },
};

/**
 * Ensures a namespace is loaded for the current language.
 * Safe to call multiple times — no-ops if already loaded.
 */
export async function loadNamespace(ns: string): Promise<void> {
  for (const lang of SUPPORTED_LANGS) {
    if (!i18n.hasResourceBundle(lang, ns)) {
      const loader = lazyNamespaces[lang]?.[ns];
      if (loader) {
        const mod = await loader();
        i18n.addResourceBundle(lang, ns, mod.default || mod, true, true);
      }
    }
  }
}
```

**Step 2: Create a useNamespace hook**

Create `src/hooks/useNamespace.ts`:
```typescript
import { useEffect, useState } from 'react';
import { useTranslation } from 'react-i18next';
import { loadNamespace } from '../i18n';

/**
 * Loads a lazy namespace and returns { t, ready }.
 * Components should check `ready` before rendering translated content.
 */
export function useNamespace(ns: string) {
  const { t, i18n } = useTranslation(ns);
  const [ready, setReady] = useState(i18n.hasResourceBundle(i18n.language, ns));

  useEffect(() => {
    if (!ready) {
      loadNamespace(ns).then(() => setReady(true));
    }
  }, [ns, ready]);

  // Re-check on language change
  useEffect(() => {
    const check = () => {
      if (!i18n.hasResourceBundle(i18n.language, ns)) {
        setReady(false);
        loadNamespace(ns).then(() => setReady(true));
      }
    };
    i18n.on('languageChanged', check);
    return () => { i18n.off('languageChanged', check); };
  }, [ns, i18n]);

  return { t, ready };
}
```

**Step 3: Create public.json files**

Create `src/locales/en/public.json` with all text extracted from the 21 public pages. This will be a large file (~300+ keys). Structure by page:

```json
{
  "landing.hero.title": "Trade Sustainable Marine Fuels with Confidence",
  "landing.hero.subtitle": "The compliance-first marketplace connecting fuel producers with maritime buyers.",
  "landing.whyCards.0.title": "Price Discovery",
  "landing.whyCards.0.body": "Real-time aggregation of supply and demand drives transparent, market-driven pricing for sustainable marine fuels.",
  "landing.whyCards.1.title": "...",
  "landing.howSteps.0.title": "...",
  "landing.stats.0.label": "...",
  "howItWorks.hero.title": "...",
  "compliance.hero.title": "...",
  "...": "..."
}
```

Create corresponding `src/locales/zh/public.json` with Chinese translations.

**Note to implementer:** Extract ALL hardcoded strings from each public page file. For each page, read the file, identify every user-facing string, create a key, and add both English and Chinese values. Use the page name as prefix (e.g., `landing.*`, `howItWorks.*`, `compliance.*`, `buyerUseCase.*`).

**Step 4: Integrate into each public page**

For each page (e.g., `LandingPage.tsx`):

```typescript
import { useNamespace } from '../../hooks/useNamespace';

export default function LandingPage() {
  const { t, ready } = useNamespace('public');
  if (!ready) return null; // or a skeleton

  // Replace all hardcoded strings:
  // Before: <h1>Trade Sustainable Marine Fuels with Confidence</h1>
  // After:  <h1>{t('landing.hero.title')}</h1>
```

Repeat for all 21 public pages.

**Step 5: Verify build and test**

Run: `cd /home/verdaxis-prod/verdaxis-frontend && npm run build && npm run test`
Fix any test failures (tests may need `I18nextProvider` wrapper in render calls).

**Step 6: Commit**

```bash
git add src/locales/en/public.json src/locales/zh/public.json src/i18n.ts src/hooks/useNamespace.ts src/pages/public/
git commit -m "feat(i18n): translate all 21 public pages to English + Chinese"
```

---

## Task 8: Translate auth flow pages

**Files:**
- Create: `src/locales/en/auth.json`
- Create: `src/locales/zh/auth.json`
- Modify: `src/pages/LoginPage.tsx`
- Modify: `src/pages/RegisterPage.tsx`
- Modify: `src/pages/ForgotPasswordPage.tsx`
- Modify: `src/pages/ResetPasswordPage.tsx`
- Modify: `src/pages/VerifyEmailPage.tsx`
- Modify: `src/pages/OnboardingPage.tsx`
- Modify: `src/pages/CreateOrganizationPage.tsx`
- Modify: `src/pages/InvitePage.tsx`
- Modify: `src/pages/KycPage.tsx`

**Step 1: Create auth.json files**

Extract all strings from the 9 auth pages. Structure:

```json
{
  "login.title": "Sign In",
  "login.subtitle": "Intelligence Cockpit",
  "login.emailLabel": "Email Address",
  "login.emailPlaceholder": "you@company.com",
  "login.passwordLabel": "Password",
  "login.forgotPassword": "Forgot password?",
  "login.noAccount": "Don't have an account?",
  "login.createAccount": "Create Account",
  "login.orContinueWith": "or continue with",
  "login.withGoogle": "Google",
  "login.withMicrosoft": "Microsoft",
  "login.error.invalidCredentials": "Invalid email or password.",
  "login.error.unverified": "Please verify your email first. Check your inbox for the verification link.",
  "login.error.serverError": "Server error. Please try again later.",
  "register.title": "Create Account",
  "register.firstNameLabel": "First Name",
  "...": "..."
}
```

Create corresponding `zh/auth.json`.

**Step 2: Integrate into each auth page**

Same pattern — `useNamespace('auth')` + replace hardcoded strings.

**Step 3: Verify build and test**

Run: `cd /home/verdaxis-prod/verdaxis-frontend && npm run build && npm run test`

**Step 4: Commit**

```bash
git add src/locales/en/auth.json src/locales/zh/auth.json src/pages/LoginPage.tsx src/pages/RegisterPage.tsx src/pages/ForgotPasswordPage.tsx src/pages/ResetPasswordPage.tsx src/pages/VerifyEmailPage.tsx src/pages/OnboardingPage.tsx src/pages/CreateOrganizationPage.tsx src/pages/InvitePage.tsx src/pages/KycPage.tsx
git commit -m "feat(i18n): translate all 9 auth flow pages to English + Chinese"
```

---

## Task 9: Translate core trading components

**Files:**
- Create: `src/locales/en/trading.json`
- Create: `src/locales/zh/trading.json`
- Modify: `src/components/Marketplace.tsx`
- Modify: `src/components/OrderBook.tsx`
- Modify: `src/components/OrderPlaceModal.tsx`
- Modify: `src/components/MyTrades.tsx`
- Modify: `src/components/TradeTape.tsx`
- Modify: `src/components/WatchlistPage.tsx`
- Modify: `src/components/MarketTerminal.tsx`
- Modify: `src/components/TradeNotifier.tsx`

**Step 1: Create trading.json files**

Extract all strings from trading components. Key areas:
- Column headers (Fuel Type, Quantity, Price, Status, etc.)
- Status labels (PENDING, CONFIRMED, DELIVERED, etc.)
- Action buttons (Place Bid, Place Ask, Confirm, Decline, etc.)
- Empty states ("No orders match your filters")
- Filter labels and options
- Toast/notification messages
- Modal titles and descriptions

Example structure:
```json
{
  "marketplace.title": "Marketplace",
  "marketplace.subtitle.buyer": "Browse available fuels and place bids",
  "marketplace.subtitle.supplier": "Manage your fuel listings",
  "marketplace.filter.fuelType": "Fuel Type",
  "marketplace.filter.region": "Region",
  "marketplace.btn.placeBid": "Place Bid",
  "marketplace.btn.placeAsk": "Place Ask",
  "marketplace.empty": "No orders match your filters",
  "orderBook.title": "Live Orderbook",
  "orderBook.bids": "Bids",
  "orderBook.asks": "Asks",
  "orderBook.buyPressure": "Buy Pressure",
  "orderBook.sellOffers": "Sell Offers",
  "orderBook.noBids": "No bids",
  "orderBook.noAsks": "No asks",
  "orderBook.spread": "Spread",
  "orderBook.crossed": "CROSSED",
  "myTrades.title": "My Trades",
  "myTrades.status.pending": "Pending",
  "myTrades.status.confirmed": "Confirmed",
  "myTrades.status.delivered": "Delivered",
  "myTrades.status.paid": "Paid",
  "myTrades.status.declined": "Declined",
  "myTrades.empty": "No trades yet",
  "...": "..."
}
```

**Step 2: Integrate into each component**

Use `useNamespace('trading')` in each component and replace all hardcoded strings.

**Step 3: Verify build and test**

Run: `cd /home/verdaxis-prod/verdaxis-frontend && npm run build && npm run test`

**Step 4: Commit**

```bash
git add src/locales/en/trading.json src/locales/zh/trading.json src/components/Marketplace.tsx src/components/OrderBook.tsx src/components/OrderPlaceModal.tsx src/components/MyTrades.tsx src/components/TradeTape.tsx src/components/WatchlistPage.tsx src/components/MarketTerminal.tsx src/components/TradeNotifier.tsx
git commit -m "feat(i18n): translate core trading components to English + Chinese"
```

---

## Task 10: Translate dashboards, fleet, and compliance

**Files:**
- Create: `src/locales/en/dashboard.json`, `src/locales/zh/dashboard.json`
- Create: `src/locales/en/fleet.json`, `src/locales/zh/fleet.json`
- Create: `src/locales/en/compliance.json`, `src/locales/zh/compliance.json`
- Modify: `src/components/BuyerDashboard.tsx`
- Modify: `src/components/BuyerMap.tsx`
- Modify: `src/components/SupplierDashboard.tsx`
- Modify: `src/components/SupplierQuotes.tsx`
- Modify: `src/components/SupplierInventory.tsx`
- Modify: `src/components/SupplierListingConsole.tsx`
- Modify: `src/components/SupplierStats.tsx`
- Modify: `src/components/SupplierAnalytics.tsx`
- Modify: `src/components/Stats.tsx`
- Modify: `src/components/Fleet.tsx`
- Modify: `src/components/Compliance.tsx`

**Step 1: Create namespace JSON files**

Extract all strings from dashboard, fleet, and compliance components into their respective namespace files with en + zh translations.

**Step 2: Integrate into each component**

Same pattern as previous tasks.

**Step 3: Verify build and test**

Run: `cd /home/verdaxis-prod/verdaxis-frontend && npm run build && npm run test`

**Step 4: Commit**

```bash
git add src/locales/en/dashboard.json src/locales/zh/dashboard.json src/locales/en/fleet.json src/locales/zh/fleet.json src/locales/en/compliance.json src/locales/zh/compliance.json src/components/BuyerDashboard.tsx src/components/BuyerMap.tsx src/components/SupplierDashboard.tsx src/components/SupplierQuotes.tsx src/components/SupplierInventory.tsx src/components/SupplierListingConsole.tsx src/components/SupplierStats.tsx src/components/SupplierAnalytics.tsx src/components/Stats.tsx src/components/Fleet.tsx src/components/Compliance.tsx
git commit -m "feat(i18n): translate dashboards, fleet, and compliance to English + Chinese"
```

---

## Task 11: Translate remaining components

**Files:**
- Create: `src/locales/en/settings.json`, `src/locales/zh/settings.json`
- Create: `src/locales/en/admin.json`, `src/locales/zh/admin.json`
- Create: `src/locales/en/ai.json`, `src/locales/zh/ai.json`
- Create: `src/locales/en/tutorial.json`, `src/locales/zh/tutorial.json`
- Create: `src/locales/en/education.json`, `src/locales/zh/education.json`
- Modify: `src/components/Settings.tsx`
- Modify: `src/components/admin/AdminDashboard.tsx`
- Modify: `src/components/ai/` (all AI copilot components)
- Modify: `src/components/GuidedTutorial.tsx`
- Modify: `src/components/Training.tsx`
- Modify: `src/components/notifications/NotificationPanel.tsx`
- Modify: `src/data/educationArticles.ts` (refactor to use i18n)
- Modify: `src/components/ErrorBoundary.tsx`
- Modify: `src/components/Toast.tsx`

**Step 1: Create namespace JSON files**

For education articles, the structure should be:
```json
{
  "articles.0.title": "What is Carbon Intensity and Why It Matters",
  "articles.0.summary": "Carbon Intensity (CI) is the single most important metric...",
  "articles.0.category": "Fundamentals",
  "articles.0.content": "Carbon Intensity (CI) measures the total greenhouse gas emissions..."
}
```

For tutorial steps:
```json
{
  "buyer.0.title": "Welcome to Verdaxis!",
  "buyer.0.content": "The compliance-first marketplace for verified sustainable marine fuels...",
  "buyer.1.title": "Intelligence Map",
  "buyer.1.content": "..."
}
```

**Step 2: Refactor educationArticles.ts**

Change from static data export to a function that reads from i18n:
```typescript
import i18n from '../i18n';

export function getEducationArticles(): EducationArticle[] {
  const t = i18n.getFixedT(null, 'education');
  return [0, 1, 2, 3, 4, 5].map(i => ({
    slug: SLUGS[i], // slugs stay in English (URL-safe)
    title: t(`articles.${i}.title`),
    summary: t(`articles.${i}.summary`),
    category: t(`articles.${i}.category`),
    readTime: READ_TIMES[i],
    content: t(`articles.${i}.content`),
  }));
}
```

**Step 3: Integrate all remaining components**

Same pattern as previous tasks.

**Step 4: Verify build and test**

Run: `cd /home/verdaxis-prod/verdaxis-frontend && npm run build && npm run test`

**Step 5: Commit**

```bash
git add src/locales/ src/components/Settings.tsx src/components/admin/ src/components/ai/ src/components/GuidedTutorial.tsx src/components/Training.tsx src/components/notifications/ src/data/educationArticles.ts src/components/ErrorBoundary.tsx src/components/Toast.tsx
git commit -m "feat(i18n): translate settings, admin, AI, tutorial, education, and remaining components"
```

---

## Task 12: Add missing translation key CI check

**Files:**
- Create: `scripts/check-translations.ts`
- Modify: `package.json` (add script)

**Step 1: Create translation checker script**

Create `scripts/check-translations.ts`:

```typescript
import fs from 'fs';
import path from 'path';

const LOCALES_DIR = path.join(__dirname, '../src/locales');
const LANGS = ['en', 'zh'];
const BASE_LANG = 'en';

let errors = 0;

// Get all namespace files from base language
const namespaces = fs.readdirSync(path.join(LOCALES_DIR, BASE_LANG))
  .filter(f => f.endsWith('.json'))
  .map(f => f.replace('.json', ''));

for (const ns of namespaces) {
  const basePath = path.join(LOCALES_DIR, BASE_LANG, `${ns}.json`);
  const baseKeys = Object.keys(JSON.parse(fs.readFileSync(basePath, 'utf-8')));

  for (const lang of LANGS) {
    if (lang === BASE_LANG) continue;
    const langPath = path.join(LOCALES_DIR, lang, `${ns}.json`);
    if (!fs.existsSync(langPath)) {
      console.error(`MISSING FILE: ${lang}/${ns}.json`);
      errors++;
      continue;
    }
    const langKeys = Object.keys(JSON.parse(fs.readFileSync(langPath, 'utf-8')));
    const missing = baseKeys.filter(k => !langKeys.includes(k));
    const extra = langKeys.filter(k => !baseKeys.includes(k));
    if (missing.length) {
      console.error(`${lang}/${ns}.json MISSING ${missing.length} keys:`, missing);
      errors += missing.length;
    }
    if (extra.length) {
      console.warn(`${lang}/${ns}.json has ${extra.length} EXTRA keys:`, extra);
    }
  }
}

if (errors > 0) {
  console.error(`\n${errors} missing translation(s) found.`);
  process.exit(1);
} else {
  console.log('All translations complete.');
}
```

**Step 2: Add npm script**

In `package.json`, add to `"scripts"`:
```json
"i18n:check": "npx tsx scripts/check-translations.ts"
```

**Step 3: Run the check**

Run: `cd /home/verdaxis-prod/verdaxis-frontend && npm run i18n:check`
Expected: "All translations complete."

**Step 4: Commit**

```bash
git add scripts/check-translations.ts package.json
git commit -m "feat(i18n): add missing translation key CI check script"
```

---

## Task 13: Update test infrastructure for i18n

**Files:**
- Create: `src/tests/test-utils.tsx`
- Modify: any failing test files

**Step 1: Create test utility wrapper**

Create `src/tests/test-utils.tsx`:

```tsx
import React from 'react';
import { render, type RenderOptions } from '@testing-library/react';
import { I18nextProvider } from 'react-i18next';
import { MemoryRouter } from 'react-router-dom';
import i18n from '../i18n';

interface WrapperProps {
  children: React.ReactNode;
}

function AllProviders({ children }: WrapperProps) {
  return (
    <I18nextProvider i18n={i18n}>
      <MemoryRouter>
        {children}
      </MemoryRouter>
    </I18nextProvider>
  );
}

export function renderWithProviders(
  ui: React.ReactElement,
  options?: Omit<RenderOptions, 'wrapper'>,
) {
  return render(ui, { wrapper: AllProviders, ...options });
}

export * from '@testing-library/react';
```

**Step 2: Update failing tests**

Any tests that render components using `useTranslation` will need the provider wrapper. Update imports from `@testing-library/react` to `../tests/test-utils` in affected test files.

**Step 3: Run full test suite**

Run: `cd /home/verdaxis-prod/verdaxis-frontend && npm run test`
Expected: All tests pass.

**Step 4: Commit**

```bash
git add src/tests/test-utils.tsx src/tests/ src/pages/public/__tests__/
git commit -m "test(i18n): add I18nextProvider test wrapper, fix all test failures"
```

---

## Task 14: Production build, deploy, and verify

**Files:**
- Modify: `index.html` (dynamic lang attribute is handled by i18n.ts already)

**Step 1: Full build**

Run:
```bash
cd /home/verdaxis-prod/verdaxis-frontend && npm run build
```
Expected: Build succeeds, no warnings about missing translations.

**Step 2: Run translation check**

Run:
```bash
cd /home/verdaxis-prod/verdaxis-frontend && npm run i18n:check
```
Expected: "All translations complete."

**Step 3: Run full test suite**

Run:
```bash
cd /home/verdaxis-prod/verdaxis-frontend && npm run test
```
Expected: All tests pass.

**Step 4: Deploy to production**

Copy built assets:
```bash
sudo cp -r /home/verdaxis-prod/verdaxis-frontend/dist/* /home/verdaxis-prod/verdaxis-frontend/dist/
```
(The Caddy config already serves from the `dist/` directory.)

**Step 5: Manual verification**

1. Visit `https://app.verdaxis.exchange/` — should redirect to `/en/`
2. Visit `https://app.verdaxis.exchange/en/` — English landing page
3. Visit `https://app.verdaxis.exchange/zh/` — Chinese landing page
4. Click language selector — should navigate between `/en/...` and `/zh/...`
5. Visit old URL `https://app.verdaxis.exchange/how-it-works` — should redirect to `/en/how-it-works`
6. Log in — app should use detected/stored language
7. Switch language in app header — should persist across page navigation
8. Refresh — language should be remembered

**Step 6: Commit any remaining fixes**

```bash
git add -A
git commit -m "feat(i18n): production build and deployment"
```
