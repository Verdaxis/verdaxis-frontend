// Test setup file for Vitest
import '@testing-library/react';
import { beforeAll, afterAll } from 'vitest';

// Global test setup
beforeAll(() => {
  // Any global setup
});

afterAll(() => {
  // Any global cleanup
});
